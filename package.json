{
  "name": "executable-microsoft-ai-golden-path",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Engineering contracts and deterministic gates, not an application runtime.",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test",
    "check": "npm run validate && npm test",
    "trace": "node scripts/validate.mjs --trace"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "yaml": "2.9.1"
  }
}